[workspace]
resolver = "2"
members = [
    "crates/eosio-chain",
    "crates/macro",
    "crates/codegen",
    # "examples/token",
]

exclude = [
    "examples/",
    "tests",
]

[profile.release]
panic = "abort"
lto = true
